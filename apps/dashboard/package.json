{
  "name": "@home-configurator/dashboard",
  "version": "0.6.8",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 0.0.0.0",
    "lint": "eslint src vite.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('Dashboard validation is covered by workspace smoke and UI contract tests')\"",
    "test:ci": "node -e \"console.log('Dashboard validation is covered by workspace smoke and UI contract tests')\""
  },
  "dependencies": {
    "@home-configurator/graphics": "workspace:*",
    "@home-configurator/home-assistant": "workspace:*",
    "@home-configurator/interaction": "workspace:*",
    "@home-configurator/runtime": "workspace:*",
    "@home-configurator/ui": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.0.5"
  }
}
