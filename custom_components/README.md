# Home Assistant Integration

The optional `home_configurator` custom integration will live here during Phase 4.

Planned responsibilities:

- config flow and one-entry setup;
- versioned configuration storage;
- optimized area/device/entity discovery;
- preset and model metadata management;
- authenticated WebSocket APIs;
- diagnostics, repairs and migrations.

The frontend must remain usable without this integration.
